import java.util.Scanner;

public class DevTest {
    public static void main(String[] args) {
        Scanner pipe = new Scanner(System.in);

        //SafeInput.getNonZeroLenString(pipe, "Hello there");

        //SafeInput.getInt(pipe, "Enter number");

        //SafeInput.getDouble(pipe, "Enter double");

        //SafeInput.getRangedInt(pipe, "Enter ranges", 1, 10);

        //SafeInput.getRangedDouble(pipe, "Enter ranges", 1.0, 10.0);

        //SafeInput.getYNConfirm(pipe, "Enter Y or N");

        //SafeInput.getRegExString(pipe, "Enter string similar to pattern", "Goofy");


    }
}
